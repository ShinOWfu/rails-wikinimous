<div>
  <h1>Welcome to Oliver's Wiki!</h1>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article), class:"text-decoration-none"%></li>
  <% end %>
  <p class="pt-50"><%= link_to "Add a new article", new_article_path %></p>
</div>